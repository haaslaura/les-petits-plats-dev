import{recipeCard}from"../templates/recipeCard.min.js";export function displayRecipes(e){const r=document.querySelector(".recipes-list");r.innerHTML="",e.forEach((e=>{const p=recipeCard(e).recipeCardTemplate();r.appendChild(p)}))}